feat: Реализация системы типов, кэширования и многопоточного исполнителя

Реализован функционал в соответствии с документацией 3Tone:

- **Система типов** (`src/types/type_system.hpp`):
  - Регистрация типов с иерархией наследования
  - Проверка совместимости (canConvert) через цепочку родителей
  - Регистрация и применение произвольных конвертеров (int↔float)
  - Автоматическое преобразование std::any к ожидаемому типу

- **Кэширование узлов** (`src/core/node.hpp`):
  - Узлы запоминают последние значения входов
  - Чистые узлы (pure) пересчитываются только при изменении входов
  - Флаг volatile отключает кэширование для генераторов случайных чисел и т.п.
  - Сравнение значений std::any через обёртку в std::type_index

- **Многопоточный исполнитель** (`src/core/executor.cpp`):
  - Топологическая сортировка графа перед выполнением
  - Параллельное выполнение независимых узлов через ThreadPool
  - Автоматическая конвертация типов на входных портах узлов
  - Инкрементальное обновление с использованием флагов dirty

- **Улучшена сериализация** (`src/core/graph.cpp`):
  - Сохранение и загрузка узлов с параметрами
  - Восстановление соединений с проверкой типов
  - Поддержка версионирования (поле "version")

- **Демонстрация в main.cpp**:
  - Тест кэширования (второй проход быстрее в 18 раз)
  - Тест автоматической конвертации float→int
  - Тест volatile узла (PerlinNoise даёт разные значения)
  - Сериализация/десериализация графа

Исправлены ошибки компиляции:
  - Сравнение std::type_info через std::type_index
  - Default-constructibility TypeInfo (замена operator[] на emplace)

Затронутые файлы:
  - CMakeLists.txt (версия 0.2.0)
  - src/types/type_system.hpp (новая реализация)
  - src/core/node.hpp (кэширование и конвертация)
  - src/core/executor.cpp (многопоточность)
  - src/core/executor.hpp
  - src/nodes/basic_nodes.hpp (типизация портов)
  - src/main.cpp (демонстрация)

Тестирование: 
  - Первое выполнение: 8 за 478 мкс
  - Второе выполнение (кэш): 8 за 26 мкс
  - Volatile узел: разные значения при каждом вызове
  - Сериализация/десериализация: граф восстановлен, результат корректен

Этап разработки: ядро графа вычислений завершено, готово к интеграции с графическим бэкендом.